import { Meta, Props, Preview, Story } from "@storybook/addon-docs";

import { story, params } from "../../utilities/story";
import CartComponent from "./Cart";

<Meta title="Components/Templates/Cart" component={CartComponent} />

# Cart example page

<Preview>
  <Story name="Cart" parameters={ params(CartComponent)} height="700px">
    {(story(CartComponent))}
  </Story>
</Preview>
